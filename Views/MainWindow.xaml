<!-- MainWindow.xaml -->
<Window x:Class="ImageProcessing.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:flowchart="clr-namespace:ImageProcessing.App.Views.Flowchart.Nodes"
        Title="Flowchart App" Height="600" Width="800">

    <Grid>
        <!-- Toolbar (optional) -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top">
            <Button Content="Add Node" Command="{Binding AddNodeCommand}"/>
        </StackPanel>

        <!-- Flowchart Canvas -->
        <ItemsControl ItemsSource="{Binding Nodes}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <flowchart:LoadImageNodeView Margin="{Binding X, Converter={StaticResource PositionConverter}}"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>

            <!-- Custom positioning -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </Grid>
</Window>